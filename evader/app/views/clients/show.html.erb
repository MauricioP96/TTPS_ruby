<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<h1>Pagina inicio</h1>
	<%= @cliente.name %>
			<%= @cliente.last_name %>
			<%= @cliente.dni %>
			<%= @cliente.birthdate %></td>
			<<%= link_to 'Modificar cliente', edit_client_path(@cliente.id) %>
			<%= link_to 'Eliminar', @cliente,method: :delete,confirm: 'Seguro quiere eliminar el cliente?' %><br>
	<%= link_to 'Atras', clients_path %>

</body>
</html>